import AddTable from '../../features/AddTable/AddTable';
import TableList from '../../features/TableList/TableList';

const Home = () => {

    return (
        <div>
           < TableList />
           < AddTable />
        </div>
    );
};

export default Home;